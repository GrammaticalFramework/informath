P. Every number is less than some number.
Q. Every number is less than the successor of some number.
R. The square of some number is less than the successor of every number.
S. Every number divides the square of some number.
T. The sum of some number and every number is less than all numbers.
U. Every number is not less than the square of some number.



